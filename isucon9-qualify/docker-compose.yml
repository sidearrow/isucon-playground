version: "3.7"
services:
  payment:
    build:
      context: .
      dockerfile: ./cmd/Dockerfile
      target: payment
    ports:
      - 15555:5555
  shipment:
    build:
      context: .
      dockerfile: ./cmd/Dockerfile
      target: shipment
    ports:
      - 17000:7000
